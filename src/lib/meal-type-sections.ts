// The URL slug for "everything not tagged with one of your dashboard
// categories" — not a real dietary_tags value, so it can never collide
// with one.
export const OTHER_CATEGORY_SLUG = "other";

// What a fresh install shows before anyone customizes it in Settings →
// קטגוריות בעמוד הראשי.
export const DEFAULT_DASHBOARD_CATEGORIES: string[] = [
  "ארוחת בוקר",
  "ארוחת צהריים",
  "ארוחת ערב",
  "קינוח",
];
